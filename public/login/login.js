(function() {
    'use strict';

    angular.module("app", [])
        .component('login', {
            controller: controller,
            templateUrl: '../login/login.html'

        })


    function controller() {
        const vm = this
    }
})();
